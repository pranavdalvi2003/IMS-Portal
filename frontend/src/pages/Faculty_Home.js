import React, { Fragment } from "react";
import Internships from "../components/internships";

const Faculty_Home = ({ authType, isAuthenticated }) => {
  return (
    <Fragment>
      <Internships />
    </Fragment>
  );
};

export default Faculty_Home;
